# Optional local env (never commit real keys).
# The Streamlit sidebar also accepts API settings per session.

# LLM_API_BASE_URL=https://your-company-gateway.example.com/v1
# LLM_API_KEY=your-api-key
# LLM_MODEL=your-model-id
