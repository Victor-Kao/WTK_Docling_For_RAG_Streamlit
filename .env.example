# Optional: copy to .env for local dev (never commit real keys).
# The Streamlit UI also accepts API keys per session (sidebar).

# GEMINI_API_KEY=your-company-gemini-key
# OPENAI_API_KEY=your-company-openai-compatible-key
# OPENAI_BASE_URL=https://your-company-gateway.example.com/v1
